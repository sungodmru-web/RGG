import type { ReactNode } from "react";
import { Link } from "wouter";

interface PagePlaceholderProps {
  eyebrow: string;
  title: string;
  description: string;
  children?: ReactNode;
}

export default function PagePlaceholder({
  eyebrow,
  title,
  description,
  children,
}: PagePlaceholderProps) {
  return (
    <main className="min-h-screen bg-[#0B0B0B] text-[#F4F1EA]">
      <section className="relative min-h-[70vh] flex items-center overflow-hidden border-b border-[#1A2E20]">
        <div
          className="
            absolute inset-0
            bg-[linear-gradient(rgba(255,255,255,0.025)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,0.025)_1px,transparent_1px)]
            bg-[size:80px_80px]
            opacity-30
            pointer-events-none
          "
        />

        <div className="absolute inset-0 bg-[radial-gradient(circle_at_70%_70%,rgba(13,59,46,0.32),transparent_45%)] pointer-events-none" />

        <div className="container mx-auto px-6 relative z-10 py-32">
          <Link
            href="/"
            className="
              inline-flex
              text-[10px]
              uppercase
              tracking-[0.24em]
              text-[#C8A96B]
              hover:text-[#EDD99A]
              transition-colors
              mb-14
            "
          >
            ← Reclaiming the Green Gold
          </Link>

          <div className="max-w-4xl">
            <p className="text-[#C8A96B] uppercase tracking-[0.28em] text-[11px] mb-6">
              {eyebrow}
            </p>

            <h1 className="font-serif text-5xl md:text-7xl lg:text-8xl leading-[0.98] tracking-[-0.03em]">
              {title}
            </h1>

            <p className="mt-8 max-w-2xl text-[#A8B2AA] text-base md:text-lg leading-8">
              {description}
            </p>

            {children && (
              <div className="mt-12">
                {children}
              </div>
            )}
          </div>
        </div>
      </section>
    </main>
  );
}